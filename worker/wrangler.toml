name = "stock-briefing-api"
main = "src/index.ts"
compatibility_date = "2026-06-01"

# 미국 실적/IPO 캐시 (cache-aside, TTL 6h). rate limit 회피용.
[[kv_namespaces]]
binding = "CACHE"
id = "a571da46fbef42ac96a5cf0957cd7b9d"

# [triggers]
# crons = ["0 * * * *"]  # 매시간 선수집(필요 시)
